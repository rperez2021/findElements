Usage:

findElement(tagName)

findElementWithText(tagName, textToSearch)

Example

```javascript
findElement('a')
// returns an array of all <a> tags in the DOM
findElementWithText(tagName, textToSearch)
```
